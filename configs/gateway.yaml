app:
  name: gateway
  env: local

http:
  host: 0.0.0.0
  port: 8080

grpc:
  core_addr: core:50051
  document_addr: document:50052

auth:
  session_ttl: 24h

max:
  api_url: https://platform-api2.max.ru
  init_data_ttl: 10m
